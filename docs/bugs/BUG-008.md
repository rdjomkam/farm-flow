# BUG-008 — Pas de lien "Sites" dans la navigation principale (bottom-nav + sidebar)
**Severite :** Moyenne
**Detecte par :** @team-lead (signale par utilisateur)
**Sprint :** 8
**Fichier(s) :** src/components/layout/bottom-nav.tsx, src/components/layout/sidebar.tsx

## Description
L'utilisateur ne peut pas naviguer vers /sites depuis n'importe quelle page. Il n'y a pas d'item "Sites" dans la bottom-nav mobile ni dans le sidebar desktop. Le seul moyen d'y acceder est de taper l'URL manuellement.

Ce qui se passe : pas d'item "Sites" dans la navigation globale.
Ce qui devrait se passer : un item "Sites" (icone Building2) dans la bottom-nav et la sidebar, href="/sites".

Note : la sidebar a peut-etre deja un item Sites (ajoute Sprint 7), mais la bottom-nav mobile ne l'a peut-etre pas. Verifier les deux.

## Etapes de reproduction
1. Ouvrir l'application sur mobile (360px)
2. Regarder la bottom-nav en bas
3. Pas d'item "Sites" visible
4. Seul moyen d'acceder a /sites : taper l'URL manuellement

## Cause racine
L'item "Sites" n'a pas ete ajoute a la navigation globale (bottom-nav et/ou sidebar) lors du Sprint 7.

## Fix applique par @developer
- [x] Fichier(s) modifie(s) : `src/components/layout/bottom-nav.tsx` — ajout item "Sites" (Building2, href="/sites")
- [x] Verifier bottom-nav.tsx — item "Sites" ajoute
- [x] Verifier sidebar.tsx — item "Sites" deja present (ajoute Sprint 7)
- [x] Icone : Building2 (lucide-react), href="/sites"
- [x] Test de non-regression : N/A (fix navigation/layout)
- [x] Build OK

## Statut : CLOS

<!-- Clos le 2026-03-09 par @project-manager. Item Sites ajoute a bottom-nav mobile. -->
