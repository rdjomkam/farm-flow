# BUG-010 — Le menu hamburger n'est pas visible dans l'application
**Severite :** Haute
**Detecte par :** @team-lead (signale par utilisateur)
**Sprint :** 8
**Fichier(s) :** src/components/layout/header.tsx, src/components/layout/app-shell.tsx

## Description
Le composant `HamburgerMenu` existe dans `src/components/layout/hamburger-menu.tsx` mais il n'est importe ni dans `header.tsx` ni dans `app-shell.tsx`. Le bouton hamburger (trois barres) n'apparait donc nulle part dans l'interface.

Ce qui se passe : pas de bouton hamburger visible sur aucune page.
Ce qui devrait se passer : un bouton hamburger dans le header, a gauche du titre, donnant acces a la navigation par modules.

## Etapes de reproduction
1. Ouvrir n'importe quelle page de l'application
2. Constater l'absence du bouton hamburger dans le header

## Cause racine
Le composant `HamburgerMenu` avait ete supprime du header lors d'une simplification externe. L'import et le rendu etaient absents.

## Fix applique par @developer
- [x] Import `HamburgerMenu` reintegre dans `src/components/layout/header.tsx`
- [x] Rendu a gauche du titre dans un flex container
- [x] Build OK

## Statut : CLOS

<!-- Clos le 2026-03-09 par @project-manager. HamburgerMenu reintegre dans le header. -->
